import { AppBar } from '@material-ui/core'
import React from 'react'

export default function Header() {
  return (
        <AppBar>
            <div>
               <h1 className="header">Worker New Shift Form </h1>
           </div>
        </AppBar>
  )
}
